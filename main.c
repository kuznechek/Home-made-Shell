#include <stdio.h>
#include "cmake-build-debug/includes/shell.c"

int main(int argc, char **argv) {
    // Загрузка файлов конфигурации при их наличии.

    // Запуск цикла команд.
    commands_loop();

    // Выключение / очистка памяти.

    return 0;
}
